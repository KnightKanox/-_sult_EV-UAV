# v0.0.1 Baseline 训练

本目录由 Docker 容器 `evuav:/root/EV-UAV/log/model/` 同步回宿主机。

同步内容主要为 baseline 训练权重：

- `models/best_iou_seed37.pt`
- `models/best_loss_seed37.pt`

由于模型权重属于大文件，已被仓库 `.gitignore` 排除，不推送到远程 Git 仓库。
