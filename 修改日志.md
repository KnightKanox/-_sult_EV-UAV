# 修改日志

## v6.0.0 — 2026-08-02 Transformer 增强版本（完整闭环）
- 新增独立版本目录 `log/versions/v6.0.0-transformer-on-v450/`，包含 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/`、`charts/` 隔离结构，基于 v4.5.0，不覆盖历史文件
- 基于 v4.5.0 保持输入固定统计归一化、GatedInputFusion、MSTB、DirectionPredictionHead、Adam、seed316、`max_events_num=50000` 和 chunked inference 不变，仅在 Bottleneck MSTB 之后插入轻量 Transformer
- Transformer 采用 token 化 + 单层 `nn.TransformerEncoder` + 坐标投影 + 门控残差回注入，token 上限 512，`transformer_num_heads=4`；修复混合精度下 dtype mismatch（float16→float32→float16 显式转换）
- 在 Docker 容器 `evuav` 内完成 prepare-config、compute-stats、quick-check、50 epochs 训练、best-IoU chunked val 评估、threshold scan、submission 生成、validate-submission 和 build-comparison；已将容器内产物同步回宿主工作区
- 50 epochs 训练：best_iou=0.4833421916259607、best_loss=0.028832606971263885、direction_aggregation_skipped=true、transformer_enabled=true；保存 `models/best_iou_seed316.pt`、`results/v600_loss_records.csv`、`results/v600_epoch_summary.csv`、`results/v600_training_summary.json`
- 阈值扫描覆盖 `0.70,0.75,0.80,0.85,0.90,0.92,0.94,0.95,0.97`：best-IoU@0.80 IoU=0.485478、seg_acc=0.874684、PD=0.879253、FA=0.001197298；lowest-FA@0.97 FA=0.000915620；recommended=0.80
- 使用 recommended threshold=0.80 生成提交候选 `submission/v600_transformer_on_v450_seed316_best_submission.zip` 和 24 个 `val_*.txt`；校验通过：zip 根目录无嵌套、字段/行数/标签/顺序均对齐、错误列表为空
- 生成 v6.0.0 与 v4.5.0、v4.3.4、v4.3.3、v4.2.0、v3.1.3、v2.1.0 同口径对比：v6.0.0 seg_acc=0.874684 与 PD=0.879253 为对比表双项最高，但 IoU=0.485478 低于 v4.3.4@0.85（0.746950）和 v4.5.0@0.80（0.720651），FA=0.001197 偏高；512 token 采样可能在 Bottleneck 丢失精细空间信息，后续可尝试增大 token_limit 或替换为全量 token 的线性注意力
- 训练结果写出后出现历史同类 `free(): invalid pointer` 退出阶段告警，不影响已写出的模型、指标、提交包和校验结果

## v4.5.0 — 2026-07-28 输入通道固定训练集统计归一化实验
- 新增独立版本目录 `log/versions/v4.5.0-input-normalization/`，包含 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/`、`charts/` 隔离结构；基于 v4.3.3 主干实现，不覆盖历史版本文件
- 基于 train split 99 个样本、9,324,544 个事件计算 8 通道 `[x,y,t,p,d,L*,C,T]` 固定统计，输出 `cache/v450_input_stats.json`，每通道包含 `mean/std/min/max/p01/p99/count`；train/val/submission 全流程复用同一份训练集统计执行 `(x - mean) / std`，`std` 带 `epsilon=1e-6` 保护，默认不启用 clipping
- 保持 v4.3.3 架构与训练 recipe：GatedInputFusion、MSTB、no PatchAttention、DirectionPredictionHead、`L_main + 0.3*L_traj + 0.2*L_dir`、Adam、seed316、`max_events_num=50000`、chunked inference；仅改输入 8 通道归一化
- 在 Docker 容器 `evuav` 内使用 `/root/exit/envs/evuav/bin/python` 完成 prepare-config、compute-stats、quick-check、50 epochs 训练、best-IoU chunked val 评估、threshold scan、submission 生成、validate-submission 和 build-comparison；已将容器内 v4.5.0 产物同步回宿主工作区
- quick-check 写入 `results/v450_quick_check.json`：记录归一化前后 train_000 样本统计，8 通道归一化特征均为有限值，模型构建兼容
- 50 epochs 训练保存 `models/best_iou_seed316.pt`、`models/best_loss_seed316.pt`、`results/v450_loss_records.csv`、`results/v450_epoch_summary.csv`、`results/v450_training_summary.json`；训练 summary：best_loss=0.026313602924346924、best_iou=0.7124943906660683、direction_aggregation_skipped=true
- best-IoU 权重在 threshold=0.90 的独立 val 评估：IoU=0.712504、seg_acc=0.848335、PD=0.835993、FA=0.000243291、TP=55571、FP=12488、FN=9935、total_events=1424330；指标写入 `results/v450_validation_metrics.json`
- 阈值扫描覆盖 `0.70,0.75,0.80,0.85,0.90,0.92,0.94,0.95,0.97`，输出 `results/v450_threshold_scan.csv`、`results/v450_threshold_scan.json`、`charts/v450_threshold_scan.csv`、`charts/v450_threshold_scan.json`、`charts/v450_threshold_scan_summary.md`、`charts/v450_threshold_scan.svg`；best-IoU threshold=0.80，IoU=0.720651、seg_acc=0.876210、PD=0.869803、FA=0.000346657；lowest-FA threshold=0.97，FA=0.000140275；best-PD threshold=0.70，PD=0.883872；综合推荐 threshold=0.90，IoU=0.712494、seg_acc=0.848335、PD=0.836203、FA=0.000243297
- 使用 recommended threshold=0.90 生成提交候选 `submission/v450_input_normalization_seed316_best_submission.zip` 和 24 个 `val_*.txt`；校验通过：zip 根目录无嵌套、字段为 `x y t p label`、标签仅 0/1、文件名匹配、行数与原始事件顺序对齐、错误列表为空
- 生成 v4.5.0 与 v4.3.4、v4.3.3、v4.2.0、v3.1.3、v2.1.0 同口径对比产物：`charts/v450_v434_v433_v420_v313_v210_comparison.csv/json/md/svg`；结论：v4.5.0 best-IoU@0.80 的 PD=0.869803 为对比表最高，但 IoU=0.720651 低于 v4.3.4@0.85、v4.3.3@0.90 和 v4.3.4@0.92，推荐@0.90 的 IoU=0.712494 也低于 v3.1.3@0.90，输入固定统计归一化未带来当前 SOTA IoU
- 训练结果写出后出现一次历史同类 `free(): invalid pointer` / `Aborted` 退出阶段告警，但模型、指标、提交包、校验和对比产物均已写出并同步；后续若继续归一化路线，可考虑 clipping 或只归一化手工特征的消融

## v4.3.4 — 2026-07-28 v4.3.3 best-IoU 阈值扫描、提交候选与消融决策
- 按“放弃 v5，尝试优化 v4”要求暂停 v5.0.0 E3/E4/E5，保留既有 E1/D2 产物，不删除用户文件
- 新增/维护独立目录 `log/versions/v4.3.4-v433-threshold-and-loss-tuning/`，包含 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/`、`charts/` 隔离结构；未覆盖 v4.3.3 文件
- 基于 v4.3.3 入口 `v433_trajectory_direction_head.py` 与 best-IoU 权重 `/root/EV-UAV/log/versions/v4.3.3-trajectory-direction-head/models/best_iou_seed316.pt` 执行阈值扫描，未修改模型结构，未启动新的 50 epoch 训练
- 阈值扫描覆盖 `0.70,0.75,0.80,0.85,0.90,0.92,0.94,0.95,0.97`，结果输出到 `results/v434_threshold_scan.csv`、`results/v434_threshold_scan_summary.json`、`charts/v434_threshold_scan.csv`、`charts/v434_threshold_scan_summary.json`、`charts/v434_threshold_scan_summary.md`、`charts/v434_threshold_scan.svg`；best-IoU threshold=0.85，IoU=0.746950，FA=0.000340180；lowest-FA threshold=0.97，FA=0.000125616；best-PD threshold=0.70，PD=0.884922；综合推荐 threshold=0.92，IoU=0.740445，seg_acc=0.832840，PD=0.798824，FA=0.000205105
- 使用综合推荐 threshold=0.92 生成提交候选 `submission/v434_v433_best_iou_threshold_tuned_submission.zip` 和 24 个 `val_*.txt`；校验通过：zip 根目录无嵌套、字段为 `x y t p label`、标签仅 0/1、总行数 1424330、前四列与原始事件顺序对齐、错误列表为空
- Task 4 工程决策为“不执行新的损失权重消融”：0.85 的 best-IoU 已较 v4.3.3@0.90 提升 +0.001865，0.92 可将 FA 从 0.000242174 降到 0.000205105，已足够支撑 v4.3.4 作为阈值调优/debug 版本；在扫描已给出明确收益且新变体需 50 epochs 成本的前提下，本轮不启动损失权重消融
- v4.3.4 本阶段作为 v4.3.3 best-IoU 权重的阈值调优/debug 版本成立；本轮未使用 Docker，容器同步不在本次任务范围内；v4.3.3 best-IoU 权重与阈值扫描输入已确认

## v5.0.0-D2 — 2026-07-28 方向头-only 诊断实验
- 新增 D2 独立诊断目录 `log/versions/v5.0.0-direction-conditioned-dual-head/d2_direction_head_only/`，隔离 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/`、`charts/` 产物，不覆盖 E1 输出
- 基于 E1 入口生成 `v500_d2_direction_head_only.py` 与配置 `configs/v500_d2_direction_head_only.yaml`，保留 v5.0.0 主干、GatedInputFusion、MSTB、DirectionPredictionHead、方向置信度和方向辅助损失，训练损失仍为 `L_main + 0.3*L_traj + 0.1*L_dir`
- 关闭 Bottleneck Local Direction-Aware Aggregation：训练和推理前向均不调用 `self.bottleneck_dir_agg(...)`，decoder 直接接收 bottleneck MSTB 输出；summary/quick-check 记录 `diagnostic=D2_direction_head_only`、`bottleneck_direction_aggregation=false`、`inference_uses_direction_aggregation=false`、`direction_aggregation_skipped=disabled_by_diagnostic`、`aggregation_apply_ratio=0.0`
- 在宿主机完成 `python3 -m py_compile` 语法检查；在 Docker 容器 `evuav` 内使用 `/root/exit/envs/evuav/bin/python3` 完成 prepare-config、quick-check、1 epoch 短训练、best-IoU chunked val 评估、submission 生成与 validate-submission
- D2 1 epoch 训练 summary：best_iou=0.0、best_loss=0.260373055934906、direction_valid_ratio=0.0007373968255706131、bottleneck_voxels=53427、aggregation_apply_ratio=0.0；训练结果写出后仍出现历史同类 `free(): invalid pointer` 退出阶段告警，但命令退出码为 0
- D2 best-IoU chunked val 指标：IoU=0.0、seg_acc=0.0、PD=0.0、FA=0.0、TP=0、FP=0、FN=65506、pred_positive=0、total_events=1424330；与 E1 相比同为零预测闭环，但 D2 明确跳过方向聚合，E1 聚合执行比例为 1.0、平均邻居数为 30.90890373781047
- 生成提交包 `submission/v500_d2_direction_head_only_seed316_best_submission.zip`，校验通过：24 个 `val_*.txt`、错误列表为空；已将容器内 D2 训练、评估、提交、校验、日志和缓存产物同步回宿主机对应目录

## v5.0.0 — 2026-07-27 Direction-Conditioned Dual Head 实现准备、E1 代码实现与闭环验证
- 新增独立版本目录 `log/versions/v5.0.0-direction-conditioned-dual-head/`，包含 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/`、`charts/` 隔离结构
- 基于 v4.3.3 生成 `v500_direction_conditioned_dual_head.py`，保留 prepare-config、quick-check、train、evaluate、submission、validate-submission 等 workflow，并切换到 v5.0.0 独立配置与输出前缀
- 新增 `configs/v500_direction_conditioned_dual_head.yaml`，记录 8 通道输入、Adam、seed316、`max_events_num=50000`、chunked inference、独立结果/模型/提交/缓存路径和初始损失 `L_main + 0.3*L_traj + 0.1*L_dir`
- 实现 E1 Local Direction-Aware Aggregation：方向头输出 L2 归一化方向和 `[0,1]` 置信度；Bottleneck 使用 `|Δx|≤1, |Δy|≤1, |Δt|≤2`、最多 44 邻居的局部查表聚合，不再使用全局 `torch.cdist(N,N)` 路径；权重融合方向一致性、时间距离和特征相似性，并使用方向置信度门控残差
- 方向 GT 调整为前景限定 PCA：优先同实例前景邻居（当 `idx_label` 映射可用），其次其他前景邻居；不足则跳过方向损失，禁止背景补足 PCA；新增 PCA confidence GT 与置信度 BCE/MSE 组合损失
- 训练/评估/quick-check summary 增加 `direction_aggregation_skipped=false`、`aggregation_apply_ratio`、`avg_neighbor_count`、`direction_valid_ratio`、`bottleneck_voxels` 等记录字段；修复混合精度下 `binary_cross_entropy` 不支持 autocast 的训练阻塞
- 在 Docker 容器 `evuav` 内使用 `/root/exit/envs/evuav/bin/python3` 完成 prepare-config 与 quick-check，确认模型构建、8 通道输入、方向头、方向置信度头、局部聚合前向兼容；quick-check 写入 `results/v500_quick_check.json`
- 完成 E1 可运行闭环训练验证：受单 batch 耗时约 1-2 分钟影响，本次执行 1 epoch 训练以验证闭环，保存 `models/best_iou_seed316.pt`、`models/best_loss_seed316.pt`、`results/v500_loss_records.csv`、`results/v500_epoch_summary.csv`、`results/v500_training_summary.json` 和 `logs/train_e1.log`；summary 记录 best_iou=0.0、best_loss=0.2614782750606537
- 使用 best-IoU 权重完成 chunked val 评估，指标写入 `results/v500_validation_metrics.json`：IoU=0.0、seg_acc=0.0、PD=0.0、FA=0.0、Bottleneck 体素数=53427、聚合执行比例=1.0、平均邻居数=30.90890373781047、方向有效体素比例=0.0007373968255706131
- 生成提交包 `submission/v500_direction_conditioned_dual_head_seed316_best_submission.zip`，校验通过：24 个 `val_*.txt`、zip 根目录无嵌套、每行 5 字段、标签仅 0/1、前四列与原始事件顺序对齐；校验报告为 `results/v500_submission_validation.json`
- 已将容器内 v5.0.0 训练、评估、提交、校验和缓存产物同步回宿主机对应目录；训练结果写出后出现历史同类 `free(): invalid pointer` 退出阶段告警，退出码为 0，不影响已写出的模型、指标、提交包和校验结果

## v3.3.2 — 2026-07-26 新增表格版版本日志
- 新增 `版本日志.md`，以表格形式按序号倒序汇总 v0.0.1 至 v3.3.1 的版本演进
- 表格包含版本号、日期、调整简述、IoU、seg_acc、PD、FA 和数值对比/结论，便于横向比较实验收益
- 补充当前关键排序：IoU 最高为 v3.1.3，seg_acc/PD 最高为 v3.1.2，FA 最低为 v2.1.0，综合低虚警高 IoU 候选为 v3.1.3

## v3.3.1 — 2026-07-26 AdamW max_events_num=60000 实验基础
- 新增独立版本目录 `log/versions/v3.3.1-adamw-max60000-seed316/`，包含 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/`、`charts/` 隔离结构
- 基于 v3.3.0 入口生成 `v331_adamw_max60000_seed316.py`，保留 8 通道四特征增强、AdamW、seed316、threshold=0.9、提交生成和提交校验逻辑
- 将默认配置名改为 `v331_adamw_max60000_seed316.yaml`，输出文件前缀改为 `v331`，版本标识改为 `v3.3.1`
- 将 `max_events_num` 从 50000 修正为 60000，并在默认配置与 `v331_training_summary.json` 中可追踪
- 已通过 `docker cp` 将宿主机版本目录同步到容器 `/root/EV-UAV/log/versions/v3.3.1-adamw-max60000-seed316/`，避免依赖 `/host_mnt`
- 在 Docker 容器 `evuav` 内使用 `/root/exit/envs/evuav/bin/python3` 完成 50 epochs 训练，保存 `models/best_iou_seed316.pt`、`models/best_loss_seed316.pt`、`results/v331_loss_records.csv`、`results/v331_epoch_summary.csv` 和 `results/v331_training_summary.json`
- v3.3.1 训练 summary：best_iou=0.42927709698869576、best_loss=0.003276865929365158、optimizer=AdamW、threshold=0.9、`max_events_num=60000`
- 使用 best-IoU 权重在 val 集完成 threshold=0.9 评估：IoU=0.42927709698869576、seg_acc=0.8278325649558819、PD=0.8383032339353212、FA=0.0012971264888161618；指标写入 `log/versions/v3.3.1-adamw-max60000-seed316/results/v331_validation_metrics.json`
- 生成提交包 `log/versions/v3.3.1-adamw-max60000-seed316/submission/v331_adamw_max60000_seed316_best_submission.zip`，校验通过：24 个 `val_*.txt`、zip 根目录无嵌套、每行 `x y t p label`、前四列与原始 `ev` 对齐、标签仅 0/1；校验报告为 `log/versions/v3.3.1-adamw-max60000-seed316/results/v331_submission_validation.json`
- 已将容器 v3.3.1 训练、评估、提交与校验产物同步回宿主机对应目录；训练、评估和提交结果写出后出现历史同类 `free(): invalid pointer` 退出阶段告警，不影响已写出的模型、指标、提交包和校验结果
- 生成 002、210、310、330、331 多版本验证指标对比产物：`log/versions/v3.3.1-adamw-max60000-seed316/charts/v002_v210_v310_v330_v331_metrics.csv`、`v002_v210_v310_v330_v331_summary.json`、`v002_v210_v310_v330_v331_summary.md`、`v002_v210_v310_v330_v331_metrics_bar.svg`；结论为 331 相比 330 仅 seg_acc 小幅提升，IoU/PD/FA 变差，综合仍不如 210，且 310 的 seg_acc 仍最高

## v3.1.3 — 2026-07-26 PatchAttention 消融实验
- 新增独立版本目录 `log/versions/v3.1.3-no-patchattention/`，包含 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/`、`charts/` 隔离结构
- 基于 v3.1.0 入口生成 `v313_no_patchattention_seed316.py`，保持 8 通道四特征、Adam、LeakyReLU、seed316、`max_events_num=50000`、50 epochs 和 threshold=0.9 设置，仅将 `pa2`、`pa3`、`pa4` 置为恒等映射，实现 no-op PatchAttention 消融
- 新增配置 `configs/v313_no_patchattention_seed316.yaml`，记录 `input_channel=8`、`max_events_num=50000`、seed316、`ablation=no_patchattention` 和隔离输出路径；quick-check 已通过，确认 8 通道前向流程兼容
- 在 Docker 容器 `evuav` 内使用 `/root/exit/envs/evuav/bin/python3` 完成 50 epochs 训练，保存 `models/best_iou_seed316.pt`、`models/best_loss_seed316.pt`、`results/v313_loss_records.csv`、`results/v313_epoch_summary.csv` 和 `results/v313_training_summary.json`
- v3.1.3 训练 summary：best_iou=0.7170857336496103、best_loss=0.0032423522789031267、ablation=no_patchattention、threshold=0.9、`max_events_num=50000`
- 使用 best-IoU 权重在 val 集完成 threshold=0.9 评估：IoU=0.7170857336496103、seg_acc=0.8076054101914328、PD=0.8097438051238975、FA=0.0001525642526742518；指标写入 `log/versions/v3.1.3-no-patchattention/results/v313_validation_metrics.json`
- 生成提交包 `log/versions/v3.1.3-no-patchattention/submission/v313_no_patchattention_seed316_best_submission.zip`，校验通过：24 个 `val_*.txt`、错误列表为空；校验报告为 `log/versions/v3.1.3-no-patchattention/results/v313_submission_validation.json`
- 生成 v3.1.0 与 v3.1.3 指标对比产物：`log/versions/v3.1.3-no-patchattention/charts/v310_v313_metrics.csv`、`v310_v313_summary.json`、`v310_v313_summary.md`、`v310_v313_metrics_bar.svg`
- 对比结论：v3.1.3 no-patchattention 相比 v3.1.0 的 IoU +0.170508、seg_acc -0.028791、PD -0.038429、FA -0.000657900；FA 越低越好，因此 v3.1.3 在 IoU 和 FA 上更优，但 seg_acc 与 PD 下降，表现为更低虚警、更保守召回
- 已将容器内训练、评估、提交和校验产物同步回宿主机；训练/评估/提交结果写出后出现历史同类 `free(): invalid pointer` / `Aborted` 退出阶段告警，不影响已写出的模型、指标、提交包和校验结果

## v3.1.2 — 2026-07-26 max_events_num=100000 阈值扫描与多版本对比
- 新增独立版本目录 `log/versions/v3.1.2-max100000-threshold-scan/`，包含 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/`、`charts/` 隔离结构
- 基于 v3.1.0 入口生成 `v312_max100000_threshold_scan.py`，保留 8 通道四特征增强、seed316、Adam、LeakyReLU 与隔离输出逻辑；新增默认配置 `configs/v312_max100000_seed316.yaml`，设置 `input_channel: 8`、`max_events_num: 100000`、`seed: 316`
- 在 Docker 容器 `evuav` 内使用 `/root/exit/envs/evuav/bin/python3` 完成 50 epochs 训练，保存 `models/best_iou_seed316.pt`、`models/best_loss_seed316.pt`、`results/v312_loss_records.csv`、`results/v312_epoch_summary.csv` 和 `results/v312_training_summary.json`；训练 summary：best_iou=0.42504317380710177、best_loss=0.0030994608532637358、`max_events_num=100000`
- 使用 best-IoU 权重执行 threshold scan，覆盖 0.70 到 0.95，输出 `results/v312_threshold_scan.csv` 与 `results/v312_threshold_scan_summary.json`；best IoU threshold=0.8，IoU=0.42958868600617545、seg_acc=0.8601807468018197、PD=0.8698026039479211、FA=0.0025289402492081293；lowest FA 候选 threshold=0.95，FA=0.001741303408006022
- 基于 threshold=0.8 生成提交包 `log/versions/v3.1.2-max100000-threshold-scan/submission/v312_max100000_seed316_best_t0p80_submission.zip`，校验通过：24 个 `val_*.txt`、错误列表为空；校验报告为 `results/v312_submission_validation.json`
- 生成 v2.1.0、v3.1.0、v3.3.0、v3.3.1、v3.1.2 多版本指标对比产物：`charts/v210_v310_v330_v331_v312_metrics.csv`、`v210_v310_v330_v331_v312_summary.json`、`v210_v310_v330_v331_v312_summary.md`、`v210_v310_v330_v331_v312_metrics_bar.svg`
- 对比结论：v3.1.2 在本组中 seg_acc 与 PD 最高，但 IoU 仅略高于 v3.3.1 且低于 v2.1.0/v3.1.0/v3.3.0，FA 为本组最高；综合最佳仍为 v2.1.0，v3.1.2 更适合作为高召回/高 seg_acc 候选
- 已将容器内训练、阈值扫描、提交校验产物同步回宿主机；训练、阈值扫描和提交结果写出后出现历史同类 `free(): invalid pointer` 退出阶段告警，不影响已写出的模型、指标、提交包和校验结果

## v3.3.0 — 2026-07-26 AdamW seed316 实验训练
- 新增独立版本目录 `log/versions/v3.3.0-adamw-seed316/`，包含 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/`、`charts/` 隔离结构
- 基于 v3.1.0/310 思路与 v3.2.0 隔离输出改法生成入口 `v330_adamw_seed316.py`，保留 8 通道四特征、`max_events_num=50000`、seed316、threshold=0.9、提交生成和提交校验逻辑
- 将默认配置名改为 `v330_adamw_seed316.yaml`，输出文件前缀改为 `v330`，版本标识改为 `v3.3.0`
- 将训练优化器从 Adam 改为 AdamW，并在配置与训练/评估/quick-check summary 中记录 `optimizer: AdamW`
- 在 Docker 容器 `evuav` 内使用 `/root/exit/envs/evuav/bin/python3` 完成 50 epochs 训练，保存 `models/best_iou_seed316.pt`、`models/best_loss_seed316.pt`、`results/v330_loss_records.csv`、`results/v330_epoch_summary.csv` 和 `results/v330_training_summary.json`
- v3.3.0 训练 summary：best_iou=0.5642375407836719、best_loss=0.0031930129043757915、optimizer=AdamW，阈值为 0.9，`max_events_num=50000`
- 使用 best-IoU 权重在 val 集完成 threshold=0.9 评估：IoU=0.5642375407836719、seg_acc=0.826321253014991、PD=0.8387232255354893、FA=0.0008144640925462252
- 生成提交包 `log/versions/v3.3.0-adamw-seed316/submission/v330_adamw_seed316_best_submission.zip`，校验通过：24 个 `val_*.txt`、zip 根目录无嵌套、每行 `x y t p label`、前四列与原始 `ev` 对齐、标签仅 0/1；校验报告为 `log/versions/v3.3.0-adamw-seed316/results/v330_submission_validation.json`
- 生成 002、210、310、330 四版本验证指标对比产物：`log/versions/v3.3.0-adamw-seed316/charts/v002_v210_v310_v330_metrics.csv`、`v002_v210_v310_v330_summary.json`、`v002_v210_v310_v330_summary.md`、`v002_v210_v310_v330_metrics_bar.svg`
- 四版本对比结论：330 相比 310 的 IoU +0.017659，但 seg_acc -0.010075、PD -0.009450、FA +0.000004；330 仅 seg_acc 高于 210，IoU、PD、FA 仍落后，综合最佳仍为 v2.1.0；各指标最优为 IoU/PD/FA=v2.1.0，seg_acc=v3.1.0
- 已通过 `docker cp` 从宿主机同步版本目录到容器 `/root/EV-UAV/log/versions/v3.3.0-adamw-seed316/`，评估、提交和校验完成后将容器产物同步回宿主机对应目录；训练与提交结果写出后出现历史同类 `free(): invalid pointer` 退出阶段告警，不影响已写出的模型、指标、提交包和校验结果

## v3.2.0 — 2026-07-26 Adagrad seed316 实验训练
- 新增独立版本目录 `log/versions/v3.2.0-adagrad-seed316/`，包含 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/`、`charts/` 隔离结构
- 基于 v3.1.0 `v310_max50000_seed316.py` 生成入口 `v320_adagrad_seed316.py`，保留 8 通道四特征、`max_events_num=50000`、seed316、提交生成与提交校验逻辑
- 将默认配置名改为 `v320_adagrad_seed316.yaml`，输出文件前缀改为 `v320`，版本标识改为 `v3.2.0`，模型文件名继续使用 `best_iou_seed316.pt` / `best_loss_seed316.pt`
- 将训练优化器从 Adam 改为 Adagrad，并在配置和训练 summary 中记录 `optimizer: Adagrad`
- 在 Docker 容器 `evuav` 内使用 `/root/exit/envs/evuav/bin/python3` 完成 50 epochs 训练，保存 `models/best_iou_seed316.pt`、`models/best_loss_seed316.pt`、`results/v320_loss_records.csv`、`results/v320_epoch_summary.csv` 和 `results/v320_training_summary.json`
- v3.2.0 训练 summary：best_iou=0.012778814113295388、best_loss=0.132899671792984，阈值为 0.9，`max_events_num=50000`
- 使用 best-IoU 权重在 val 集完成 threshold=0.9 评估：IoU=0.012778814113295388、seg_acc=0.02447104082068818、PD=0.1127677446451071、FA=0.0011568189273413155
- 生成提交包 `log/versions/v3.2.0-adagrad-seed316/submission/v320_adagrad_seed316_best_submission.zip`，校验通过：24 个 `val_*.txt`、zip 根目录无嵌套、每行 `x y t p label`、前四列与原始 `ev` 对齐、标签仅 0/1
- 生成 002、210、310、320 四版本对比图表，记录 v3.2.0 Adagrad 相比既有版本明显退化
- 已从容器 `/root/EV-UAV/log/versions/v3.2.0-adagrad-seed316/` 同步训练、评估与提交产物回宿主机对应目录；训练/评估/提交结果写出后出现历史同类 `free(): invalid pointer` 退出阶段告警，不影响已写出的模型、指标、提交包和校验结果

## v3.1.1 — 2026-07-26 002/210/310 三版本指标对比图
- 在 `log/versions/v3.1.0-max50000-seed316/charts/` 生成三版本验证指标对比产物：`v002_v210_v310_metrics_bar.svg`、`v002_v210_v310_metrics.csv`、`v002_v210_v310_summary.json`、`v002_v210_v310_summary.md`
- 对比 v0.0.2 LeakyReLU、v2.1.0 `v002_slope_0p005` 与 v3.1.0 `max50000_seed316` 在 IoU、seg_acc、PD、FA 四项指标上的表现
- 结论：210 的 IoU=0.652904、PD=0.862873、FA=0.000108761 综合最优；310 的 seg_acc=0.836397 最高；310 相比 002 明显提升，但尚未超过 210 综合表现

## v3.1.0 — 2026-07-26 max_events_num=50000 seed316 四特征增强训练
- 新增独立版本目录 `log/versions/v3.1.0-max50000-seed316/`，隔离保存 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/`、`charts/` 产物，未覆盖 v3.0.0 产物
- 基于 v3.0.0 8 通道四特征增强 EV-SpSegNet，配置保持 `input_channel: 8`，将 `max_events_num` 从 20000 提升到 50000，随机种子从 37 改为 316
- 在 Docker 容器 `evuav` 内使用 `/root/exit/envs/evuav/bin/python3` 完成 50 epochs 训练，保存 `models/best_iou_seed316.pt`、`models/best_loss_seed316.pt`、`results/v310_loss_records.csv`、`results/v310_epoch_summary.csv` 和 `results/v310_training_summary.json`
- v3.1.0 验证集指标：IoU=0.546578、seg_acc=0.836397、PD=0.848173、FA=0.000810464，阈值为 0.9，best_loss=0.00306768
- 生成提交包 `log/versions/v3.1.0-max50000-seed316/submission/v310_max50000_seed316_best_submission.zip`，校验通过：24 个 `val_*.txt`、zip 根目录无嵌套、每行 `x y t p label`、前四列与原始 `ev` 对齐、标签仅 0/1
- 生成 v3.1.0 vs v3.0.0 比较产物 `charts/v310_vs_v300_metrics.csv`、`v310_vs_v300_metrics_bar.svg`、`v310_vs_v300_delta.svg`、`v310_vs_v300_summary.json`、`v310_vs_v300_summary.md`；相对 v3.0.0：IoU +0.151928、seg_acc +0.032028、PD +0.024570、FA -0.000731
- 已从容器 `/root/EV-UAV/log/versions/v3.1.0-max50000-seed316/` 同步完整产物回宿主机对应目录；训练、验证和提交命令在结果写出后仍出现历史同类 `free(): invalid pointer` 退出阶段告警，不影响已写出的模型、指标和提交校验结果

## v3.0.1 — 2026-07-25 v3.0.0 与 v0.0.2 指标比较图
- 新增 `log/versions/v3.0.0-handcrafted-feature-evspsegnet/charts/` 对比产物目录
- 生成 `v300_vs_v002_metrics_bar.svg`，对比 v0.0.2 LeakyReLU 与 v3.0.0 d/L*/C/T 在 IoU、seg_acc、PD、FA 四项验证指标上的表现
- 生成 `v300_vs_v002_delta.svg`，展示 v3.0.0 相对 v0.0.2 的指标增量：IoU +0.043540、seg_acc +0.094342、PD +0.027300、FA +0.000267
- 生成 `v300_vs_v002_metrics.csv`、`v300_vs_v002_summary.json` 和 `v300_vs_v002_summary.md`，记录结构化比较数据与结论：v3.0.0 提升目标覆盖和召回，但虚警略高

## v3.0.0 — 2026-07-25 手工时空特征增强 EV-SpSegNet
- 新增独立版本目录 `log/versions/v3.0.0-handcrafted-feature-evspsegnet/`，包含 `configs/`、`results/`、`models/`、`submission/`、`logs/`、`cache/` 等隔离产物目录
- 新增 `v300_handcrafted_feature_evspsegnet.py`，支持 `prepare-config`、`quick-check`、`train`、`evaluate`、`submission`、`validate-submission` 子命令
- 在 v3 入口内实现 8 通道数据路径：原始 `evs_norm[:, 0:4]` 加手工特征 `[d, L*, C, T]`，并保持历史 v0.x/v2.x 数据集和训练脚本不变
- 四类特征包含高斯加权局部密度 d、PCA 可靠线性度 L*、极性一致性 C、局部时间连续性 T；容器 quick-check 验证 `train_000` 的 39169 个事件特征均有限且落在 `[0,1]`
- 在 Docker 容器 `evuav` 内使用 `/root/exit/envs/evuav/bin/python3` 完成 50 epochs 训练，保存 `models/best_iou_seed37.pt`、`models/best_loss_seed37.pt`、`results/v300_loss_records.csv`、`results/v300_epoch_summary.csv` 和训练 summary
- v3.0.0 验证集指标：IoU=0.394651、seg_acc=0.804369、PD=0.823604、FA=0.00154179，阈值为 0.9
- 生成提交包 `log/versions/v3.0.0-handcrafted-feature-evspsegnet/submission/v300_handcrafted_feature_best_submission.zip`，校验通过：24 个 txt、zip 根目录无嵌套、每行 `x y t p label`、前四列与原始 `ev` 对齐、标签仅 0/1
- 已从容器 `/root/EV-UAV/log/versions/v3.0.0-handcrafted-feature-evspsegnet/` 同步完整产物回宿主机对应目录；训练、验证和提交命令在结果写出后仍出现历史同类 `free(): invalid pointer` 退出阶段告警，不影响已写出的模型、指标和提交校验结果

## v2.1.0 — 2026-07-25 v0.0.2 改进实验套件
- 新增版本目录 `log/versions/v2.1.0-v002-improvement-suite/`，将围绕当前最佳 v0.0.2 LeakyReLU 的后续构建方向落成统一实验入口
- 新增 `v002_improvement_suite.py`，支持阈值细扫、验证集提交生成、D 组样本池复训、正类召回增强、STC 参数网格、传统后处理融合和 LeakyReLU 斜率微调
- 生成 6 个训练变体配置与 `run_plan.txt`：`v002_d_pool_retrain`、`v002_recall_pos2_dice02`、`v002_stc_t3`、`v002_stc_t7`、`v002_slope_0p005`、`v002_slope_0p02`
- 新增 `results/planned_experiments.csv` 与 `说明.md`，记录推荐执行顺序、输出目录和 Docker/conda 环境要求
- 已在宿主机完成脚本语法检查和轻量配置生成；实际阈值扫描、融合扫描和训练仍需在 EV-UAV Docker/conda 环境内执行
- 已完成 6 组 50 epochs 训练并生成训练图表、验证指标对比图和最终汇总；最佳变体为 `v002_slope_0p005`，验证 IoU=0.652904、seg_acc=0.810155、PD=0.862873、FA=0.000108761，相比 v0.0.2 的 IoU=0.351111 明显提升
- 生成最佳模型提交包 `log/versions/v2.1.0-v002-improvement-suite/submission/v002_slope_0p005_best_submission.zip`，校验通过：24 个 txt、zip 无嵌套目录

## v2.0.0 — 2026-07-25 二维视觉训练识别管线
- 新增架构版本目录 `log/versions/v2.0.0-2d-vision-pipeline/`，将三维事件流确定性压缩为二维 RGB 投影图并保留像素到原始事件点映射
- 为 train 99 个样本生成二维图片、完整二值 mask、映射文件和 `compression_index.csv`；为 val 24 个样本生成二维图片和映射文件
- 采用 PyTorch SmallUNet/U-Net 二分类语义分割模型完成二维视觉管线 50 epochs 全量训练，配置为 batch_size=4、threshold=0.5，并保存权重 `model/small_unet_final.pt`，最终 mean_loss=0.04851839609444141
- 已完成 50 epochs 全量训练并重新生成 24 个验证样本二维 mask 推理结果，按映射逆向还原为事件级 `submission/val_*.txt`，每行格式为 `x y t p label`
- 生成无嵌套提交压缩包 `archive/v2.0.0-2d-vision-pipeline_submission.zip`，验证通过：24 个 txt、文件名匹配、行数与原始 val 事件数一致、每行 5 字段、标签仅 0/1、zip 无子目录
- 生成 `解释文档.md`、训练配置/日志、预测索引和验证报告，记录数据来源、压缩规则、模型训练、推理阈值、逆向还原与执行结果

## v0.1.0 — 2026-07-24 LeakyReLU 样本池配比训练与对比
- 使用当前 LeakyReLU 模型权重对训练集 99 个 npz 执行推理统计，按背景误报比例和时空集中度分类为困难负样本、时空结构背景、均匀背景各 33 个
- 构建 A/B/C/D 四个样本池，每个池 99 个样本（含重复），各组配比：
  - A（偏均匀）：25% 困难负样本 / 25% 时空结构背景 / 50% 均匀背景
  - B（平衡版）：35% / 30% / 35%
  - C（偏困难负样本）：45% / 30% / 25%
  - D（偏时空结构）：35% / 40% / 25%
- 修复 `utils/eval.py` 中 PyTorch 1.9 CUDA 布尔索引 bug（eval 时先在 CPU 做阈值操作再移 GPU）
- 分别在 A/B/C/D 样本池上完成 50 epochs 全量训练，batch_size=1, max_events_num=20000
- 在验证集（24 样本）上评估四组模型，threshold=0.9：
  | 组别 | IoU | Seg Acc | PD | FA |
  |------|-----|---------|-----|-----|
  | A (25/25/50) | 0.3577 | 0.7228 | 0.8049 | 0.00126 |
  | B (35/30/35) | 0.3715 | 0.7097 | 0.8022 | 0.00096 |
  | C (45/30/25) | 0.3450 | 0.7101 | 0.8123 | 0.00237 |
  | D (35/40/25) | 0.3731 | 0.7174 | 0.8026 | 0.00105 |
- 关键结论：D 组（偏时空结构）IoU 最高 0.3731；B 组（平衡版）FA 最低 0.00096；C 组（偏困难负样本）PD 最高但 IoU 最低；时空结构背景比例提升对分割精度最有利
- 将 v0.0.2 LeakyReLU 权重从容器 `/root/EV-UAV/log/model_leaky_relu/` 归档到 `log/versions/v0.0.2-leaky-relu-training/models/`
- 产物目录 `log/versions/0.1.0-sample-pool-ratio-experiments/`：
  - `charts/`：`group_epoch_loss_comparison.png`、`group_val_metrics_bar.png`、`group_pd_fa_comparison.png`
  - `group_val_results.csv`、`group_val_summary.txt`：四组评估汇总
  - `A/B/C/D/models/`：各自 `best_iou_seed37.pt` 和 `best_loss_seed37.pt`
  - `A/B/C/D/loss_records.csv`、`epoch_loss_summary.csv`：结构化训练日志

## v0.0.6 — 2026-07-24 日志产物版本化整理
- 统一采用标准三位版本号格式：`vMAJOR.MINOR.PATCH`，当前历史版本调整为 `v0.0.x`
- 按版本号整理日志与比较产物：
  - `log/versions/v0.0.1-baseline-training/`：Baseline 权重归档
  - `log/versions/v0.0.2-leaky-relu-training/`：LeakyReLU 全量训练日志、参数图表与权重归档
  - `log/versions/v0.0.3-val-compare/`：Baseline vs LeakyReLU 验证集四指标对比
  - `log/versions/v0.0.5-threshold-scan/`：阈值扫描、同 PD 下 FA 对比、同 FA 下 PD 对比
- 更新 `现状描述.md` 中所有日志产物路径，统一指向 `log/versions/v0.0.x-*` 版本化目录
- 更新快速验证命令中的图表目录路径
- 保持修改日志按版本号降序排列

## v0.0.5 — 2026-07-24 阈值扫描与同 PD/同 FA 对比
- 对 baseline(ReLU) 与 LeakyReLU 在验证集 val 上扫描阈值 `0.05..0.95`，步长 `0.05`
- 复用 EV-UAV 原始 `utils/eval.py` 的 iou、seg_acc、pd、fa 指标逻辑
- 生成 `log/versions/v0.0.5-threshold-scan/results/` 产物：
  - `pd_fa_curve.png`：PD-FA 曲线
  - `metrics_vs_threshold.png`：阈值-指标曲线
  - `fa_at_same_pd.png`：同 PD 下 FA 对比
  - `pd_at_same_fa.png`：同 FA 下 PD 对比
  - `threshold_scan_all.csv`、`same_pd_compare.csv`、`same_fa_compare.csv` 等结构化结果
- 关键结论：
  - 同 PD=0.75/0.80/0.85 时，LeakyReLU 的 FA 均更低
  - 同 FA=0.002/0.005/0.010 时，LeakyReLU 的 PD 均更高
  - FA=0.001 低于当前扫描结果可插值范围，未给出同 FA 下 PD
- 更新 `现状描述.md` 的结果汇总章节，加入阈值扫描表格和产物说明

## v0.0.4 — 2026-07-24 维护现状描述与日志
- 更新 `现状描述.md`，补充 `log/` 目录结构、LeakyReLU 训练产物、验证集对比结果和 Git 远程状态
- 补充 LeakyReLU 训练记录：50 epochs、loss 记录数 9950、Epoch 0/49 平均 loss、943,749 参数量
- 补充验证集对比结论：LeakyReLU IoU +0.0271，FA -0.0014，Seg Acc/Pd 略低
- 补充已知问题：`free(): invalid pointer` 退出阶段告警、公平对比需匹配模型结构与权重
- 更新快速验证命令，加入 LeakyReLU 权重检查和图表目录查看命令

## v0.0.3 — 2026-07-24 验证集对比：Baseline (ReLU) vs LeakyReLU
- 分别加载 baseline (`best_iou_seed37.pt`, ReLU) 与 LeakyReLU (`best_iou_seed37.pt`, LeakyReLU(0.01)) 权重
- 在验证集 (val, 24 样本) 上评估 iou、seg_acc、pd、fa 四项指标
- 生成对比图表和 CSV 汇总，存放于 `log/versions/v0.0.3-val-compare/results/`
  - `val_metrics_comparison.png`：四指标分组柱状图
  - `val_metrics_combined.png`：合并柱状图
  - `val_compare_results.csv`：指标表格
  - `val_compare_summary.txt`：文字汇总含 Delta
- 关键结论：
  - LeakyReLU IoU +0.0271 (0.324→0.351)，但 seg_acc -0.0072, pd -0.0172
  - LeakyReLU FA 减半 (0.0027→0.0013)，误检更少

## v0.0.2 — 2026-07-24 LeakyReLU 全量训练
- 创建分支 `experiment/leaky-relu`，将模型激活函数从 ReLU 改为 LeakyReLU(negative_slope=0.01)
- 涉及文件：`model/evspsegnet.py`, `model/basemodel.py`
- 配置 `max_events_num=20000`, batch_size=1, 训练 50 epochs
- 模型保存到独立目录 `log/model_leaky_relu/`，不覆盖 baseline 权重
- 训练日志保存至 `log/versions/v0.0.2-leaky-relu-training/leaky_relu_train.log`
- 生成训练图表存放于 `log/versions/v0.0.2-leaky-relu-training/charts/`：
  - `batch_loss_curve.png`：batch loss 曲线 (9950 条记录)
  - `epoch_mean_loss_curve.png`：epoch 平均 loss (0.234→0.023)
  - `parameter_distribution.png` / `parameter_total_trainable.png`：参数分布 (943,749 parameters, all trainable)
  - CSV 和 run_summary.txt 汇总

## v0.0.1 — 2026-07-23 Baseline 跑通
- Docker 环境配置 (`evuav` 容器, CUDA 11.3, PyTorch 1.10.1+cu113)
- 配置文件路径适配 Docker 容器 (`configs/evisseg_evuav.yaml`, `configs/configs.py`)
- spconv CUDA kernel 架构适配 (RTX 2060 SUPER sm_75, 改用 Native 算法)
- `max_events_num` 从 150000 降到 20000 (8GB 显存 OOM 适配)
- Baseline 训练完成 (50 epochs)，生成 `log/model/best_iou_seed37.pt`
- 测试指标：iou=0.332, seg_acc=0.738, pd=0.859, fa=0.010
- 生成项目文档 `现状描述.md`
