# EV-UAV 项目现状描述

## 1. 项目概述

EV-UAV 是 ICCV 2025 论文 **"Event-based Tiny Object Detection: A Benchmark Dataset and Baseline"** 的官方实现。项目面向基于事件相机的反无人机小目标检测与事件级分割任务。

- 任务：事件相机数据下的无人机小目标检测/事件级语义分割
- 数据集：EV-UAV，包含 147 个事件序列，超过 230 万个事件级标注
- UAV 平均目标尺寸约 `6.8 × 5.4` 像素（极低分辨率场景）
- 基线方法：EV-SpSegNet，基于稀疏事件点云分割网络 + STC 时空相关性损失函数
- 项目路径（宿主机）：`/media/sult/山江冬夏/训练集、验证集/EV-UAV`
- 项目路径（Docker 容器）：`/root/EV-UAV`

---

## 2. 项目目录结构

```
EV-UAV/
├── configs/
│   ├── configs.py            # 配置解析脚本（argparse + yaml）
│   └── evisseg_evuav.yaml    # 核心配置文件
├── dataset/
│   ├── basedataset.py         # 基础数据集类 + 体素化函数 + custom_collate
│   └── ev_uav.py              # EV-UAV 数据集加载类
├── model/
│   ├── basemodel.py           # 基础模块（GDBlock、Downsample_block 等）
│   └── evspsegnet.py          # 主干网络 EV-SpSegNet（编码器-解码器）
├── utils/
│   ├── eval.py                # 评估函数（mIoU、seg_acc、Pd、Fa）
│   ├── stcloss.py             # STC 时空相关性损失函数
│   └── vis.py                 # 可视化工具
├── lib/
│   └── hais_ops/              # 自定义 C++/CUDA 扩展算子（体素化、IoU、ROI Pooling 等）
├── imgs/                      # README 插图资源
├── log/                       # 训练日志、模型结果图表、验证集对比产物
│   ├── versions/              # 按标准三位版本号归档的实验产物
│   │   ├── v0.0.2-leaky-relu-training/   # LeakyReLU 全量训练日志与图表
│   │   ├── v0.0.3-val-compare/           # Baseline vs LeakyReLU 验证集四指标对比
│   │   ├── v0.0.5-threshold-scan/        # 阈值扫描与同 PD/同 FA 对比
│   │   └── 0.1.0-sample-pool-ratio-experiments/  # 样本池配比训练与对比
│   │       ├── classification/    # 样本分类结果（困难负样本/时空结构背景/均匀背景）
│   │       ├── A/ B/ C/ D/        # 四组样本池（train/ + models/ + logs/ + config.yaml）
│   │       ├── charts/            # 对比图表（loss曲线、指标柱状图、PD-FA对比）
│   │       ├── group_val_results.csv / group_val_summary.txt
│   │       └── run_group_experiments.py  # 自动化训练评估脚本
│   └── model*/                # 容器内模型保存目录，宿主机不一定完整同步
├── train.py                   # 训练入口脚本
├── test.py                    # 测试入口脚本
├── Dockerfile                 # Docker 环境构建文件
├── docker_for_evuav.md        # Docker 使用说明
├── debug-spconv-kernel.md     # spconv kernel 调试笔记
├── 修改日志.md                # 当前维护日志，按版本降序排列
└── README.md                  # 原始项目说明（已翻译为中文）
```

---

## 3. 运行环境

### 3.1 Docker 容器

项目通过 Docker 容器 `evuav` 运行，基础镜像为：

```
nvidia/cuda:11.3.1-cudnn8-devel-ubuntu20.04
```

### 3.2 关键依赖版本

| 组件 | 版本 | 说明 |
|------|------|------|
| Python | 3.8.20 | conda 环境 `evuav` |
| PyTorch | 1.9.1+cu111 | 训练/推理框架 |
| CUDA | 11.3（系统）/ 11.1（torch） | torch.__version__.cuda = 11.1 |
| cuDNN | 8.2.1 | — |
| spconv | 2.1.21（spconv-cu111） | 稀疏卷积库 |
| cumm | 0.2.9（cumm-cu111） | CUDA 集合运算库（spconv 依赖） |
| ccmport / pccm | 0.3.x | spconv 依赖 |
| HAIS_OP | 自定义编译 | 体素化、IoU 等 CUDA 扩展 |
| numpy | 1.21.6 | — |
| matplotlib | 3.5.3 | — |
| scipy | 1.7.3 | — |
| tensorboardX | 2.6.2 | — |
| PyYAML | 6.0 | — |
| opencv-python | 4.8.0.76 | — |
| numba | 0.56.4 | — |
| GCC | 9.x | CUDA 11.3 编译要求 |

### 3.3 GPU 信息

| 属性 | 值 |
|------|-----|
| GPU 型号 | NVIDIA GeForce RTX 2060 SUPER |
| 显存 | 8192 MiB（8 GB） |
| CUDA Compute Capability | 7.5（sm_75） |
| Driver 版本 | 595.71.05 |

### 3.4 环境注意事项

- 容器默认 `/usr/bin/python3` 没有安装 torch/spconv 等依赖，**必须使用 conda 环境**：
  - conda 环境名：`evuav`
  - Python 路径：`/root/exit/envs/evuav/bin/python`
- Dockerfile 中声明的工作目录为 `/workspace`，但当前容器实际项目路径为 `/root/EV-UAV`
- 数据集挂载路径：`/root/EV-UAV-dataset`

---

## 4. 运行使用指令

### 4.1 环境启动

```bash
# 启动容器（宿主机执行）
docker start evuav

# 进入容器交互式 shell
docker exec -it evuav bash

# 激活 conda 环境（容器内）
conda activate evuav
cd /root/EV-UAV
```

### 4.2 训练

```bash
# 在容器内执行
conda activate evuav
cd /root/EV-UAV
python train.py
```

说明：
- 默认读取配置文件 `/root/EV-UAV/configs/evisseg_evuav.yaml`
- 支持通过命令行指定配置：`python train.py --config /path/to/config.yaml`
- 训练 50 epochs，batch_size=1
- 模型保存到 `/root/EV-UAV/log/model/`

### 4.3 测试

```bash
# 在容器内执行
conda activate evuav
cd /root/EV-UAV
python test.py
```

### 4.4 宿主机快捷执行

```bash
# 在宿主机执行训练
docker exec evuav bash -i -c "conda activate evuav && cd /root/EV-UAV && python train.py"

# 在宿主机执行测试
docker exec evuav bash -i -c "conda activate evuav && cd /root/EV-UAV && python test.py"
```

---

## 5. 数据集

### 5.1 目录结构

```
/root/EV-UAV-dataset/
├── train/          # 99 个 .npz 文件
├── val/            # 24 个 .npz 文件
└── test/           # 24 个 .npz 文件
```

合计 **147 个事件序列**。

### 5.2 npz 文件字段

每个 `.npz` 文件包含多个数组，代码中主要读取以下字段：

| 字段 | 含义 | 形状 | 代码使用方式 |
|------|------|------|-------------|
| `ev` | 原始事件数据 | — | — |
| `evs_norm` | 归一化事件数据 | `(N, 6)` | `[:, 0:4]` 为输入特征；`[:, 4]` 为分割标签；`[:, 5]` 为目标索引 |
| `ev_loc` | 事件空间坐标 | `(N, 3)` | 用于体素化/稀疏卷积 |

字段语义：
- `evs_norm[:, 0:4]`：事件特征（归一化后的 x, y, t, p 或其他四维特征）
- `evs_norm[:, 4]`：`seg_label`，二值分割标签（0=背景，1=目标）
- `evs_norm[:, 5]`：`idx`，目标实例索引
- `ev_loc`：事件点云的三维空间坐标

### 5.3 数据加载流程

1. `EvUAV` 类（`dataset/ev_uav.py`）读取指定 mode 目录下所有 npz 文件
2. 通过 `custom_collate`（`dataset/basedataset.py`）进行批处理：
   - 合并 batch 内所有事件点云
   - 调用 `HAIS_OP.voxelize_idx` 体素化
   - 调用 `HAIS_OP.voxelize_fp` 聚合体素特征
   - 构造 `spconv.SparseConvTensor`，稀疏空间形状固定为 `[11×32, 9×32, 256×32]`
3. 训练模式下，若单帧事件数超过 `max_events_num`，随机下采样至指定数量
4. 返回字段：`voxel_ev`、`seg_label`、`p2v_map`、`locs`、`idx_label`

### 5.4 数据集配置

```yaml
DATA:
  input_channel: 4
  root: /root/EV-UAV-dataset
  whole_t: 8000          # ms，时间窗口
  res: [346, 260]
```

---

## 6. 网络架构

### 6.1 总体结构

EV-SpSegNet（文件：`model/evspsegnet.py`）是基于稀疏卷积的编码器-解码器网络。

```
输入：spconv.SparseConvTensor（batch_size=1）
  │
  ├── conv_input：SubMConv3d(4, 12, 3) → BN → ReLU
  │
  ├── [编码器]
  │   ├── conv1：GDBlock（多 dilation 分组稀疏卷积，不改变空间分辨率）
  │   ├── conv2：SparseConv3d(下采样 stride=[2,2,4]) → GDBlock → PatchAttention
  │   ├── conv3：SparseConv3d(下采样 stride=[2,2,4]) → GDBlock → PatchAttention
  │   └── conv4：SparseConv3d(下采样 stride=[2,2,4]) → GDBlock → PatchAttention
  │
  ├── [解码器]
  │   ├── conv_up_t4 + conv_up_m4 + inv_conv4 → 上采样
  │   ├── conv_up_t3 + conv_up_m3 + inv_conv3 → 上采样
  │   ├── conv_up_t2 + conv_up_m2 + inv_conv2 → 上采样
  │   └── conv_up_t1 + conv_up_m1 + conv5 → 上采样
  │
  └── semantic_linear：Linear(width, 1) → Sigmoid
           │
           └── 输出：(preds, voxel)，逐体素分割概率
```

### 6.2 关键参数

| 参数 | 值 |
|------|-----|
| input_channels | 4 |
| width（基础通道数） | 12 |
| 下采样 stride | [2, 2, 4]（空间维度下采样，时间维度下采样更大） |
| 稀疏空间形状 | [352, 288, 8192] |
| 卷积算法 | `spconv.ops.ConvAlgo.Native` |

### 6.3 核心模块

- **GDBlock（`model/basemodel.py`）**：多 dilation 分组稀疏卷积模块，支持不同膨胀率的并行卷积分支，内部含 SE 注意力机制
- **PatchAttention**：对稀疏体素做多级 SparseMaxPool3d 下采样，通过 MultiheadAttention 计算全局注意力，再用 SparseInverseConv3d 上采样还原
- **SparseBasicBlock**：残差稀疏卷积块（两个 SubMConv3d + 跳跃连接）
- **UR_block_forward**：解码器基本单元，融合横向连接与底部分支特征，通道归约后上采样
- **channel_reduction**：将高维特征通过 reshape-sum 归约到目标通道数

---

## 7. 训练流程

### 7.1 训练配置

| 超参数 | 值 |
|--------|-----|
| Epochs | 50 |
| Batch Size | 1 |
| Optimizer | Adam |
| Learning Rate | 0.001 |
| LR Schedule | StepLR：每 10 epoch × 0.1 |
| 随机种子 | 37 |
| max_events_num | 20000（单帧最大事件数，为解决 8GB 显存 OOM 调低） |

确定性设置：
- `torch.use_deterministic_algorithms(True)`
- `torch.backends.cudnn.deterministic = True`
- `torch.backends.cudnn.benchmark = False`
- 环境变量 `CUBLAS_WORKSPACE_CONFIG=:16:8`

### 7.2 损失函数

组合损失：
- **BCE Loss**：`torch.nn.BCELoss()`，二值交叉熵
- **STC Loss**：`utils/stcloss.py`，时空相关性损失函数，利用事件数据的时间连续性约束，参数 `k=3, t=5`

### 7.3 训练循环

```
1. 固定随机种子 37
2. 构建 evspsegnet，train 模式，移到 GPU
3. 加载 EV-UAV train 数据集
4. DataLoader（RandomSampler，batch_size=1，custom_collate 体素化）
5. 初始化 STCLoss + BCELoss
6. Adam 优化器 + StepLR 调度器
7. 每个 epoch：
   ├── 遍历训练 batch
   │   ├── 前向传播 → (preds, voxel)
   │   ├── 计算 STC loss
   │   └── 反向传播 + optimizer.step()
   ├── 保存 loss 更低的模型 → best_loss_seed37.pt
   └── epoch >= 40 后在 val 集评估 mIoU：
       └── iou 更高时保存 → best_iou_seed37.pt
8. 训练结束，输出 best_iou_seed37.pt
```

### 7.4 模型保存

Baseline 训练完成后生成两个文件：

| 文件 | 大小 | 说明 |
|------|------|------|
| `/root/EV-UAV/log/model/best_iou_seed37.pt` | ~3.76 MB | Baseline 验证集 IoU 最高的模型 |
| `/root/EV-UAV/log/model/best_loss_seed37.pt` | ~3.76 MB | Baseline 训练 loss 最低的模型 |

LeakyReLU 实验使用独立保存目录，避免覆盖 baseline：

| 文件 | 大小 | 说明 |
|------|------|------|
| `/root/EV-UAV/log/model_leaky_relu/best_iou_seed37.pt` | ~3.8 MB | LeakyReLU 验证集 IoU 最高的模型 |
| `/root/EV-UAV/log/model_leaky_relu/best_loss_seed37.pt` | ~3.8 MB | LeakyReLU 训练 loss 最低的模型 |

### 7.5 LeakyReLU 实验训练记录

当前分支 `experiment/leaky-relu` 将网络中的 `ReLU` 替换为 `LeakyReLU(negative_slope=0.01)`，并完成 50 epochs 全量训练。

| 项目 | 值 |
|------|-----|
| 分支 | `experiment/leaky-relu` |
| 提交 | `5db21ac` |
| 训练 epochs | 50 |
| batch_size | 1 |
| max_events_num | 20000 |
| loss 记录数 | 9950 |
| Epoch 0 平均 loss | 0.2344773869346734 |
| Epoch 49 平均 loss | 0.023061306532663358 |
| 总参数量 | 943,749 |
| 可训练参数量 | 943,749 |
| 训练日志 | `log/versions/v0.0.2-leaky-relu-training/leaky_relu_train.log` |
| 图表目录 | `log/versions/v0.0.2-leaky-relu-training/charts/` |

`log/versions/v0.0.2-leaky-relu-training/charts/` 中包含：
- `batch_loss_curve.png`：batch loss 曲线
- `epoch_mean_loss_curve.png`：epoch 平均 loss 曲线
- `parameter_distribution.png`：模块参数分布
- `parameter_total_trainable.png`：总参数量与可训练参数量对比
- `loss_records.csv`、`epoch_loss_summary.csv`、`model_parameters.csv`、`run_summary.txt`：结构化数据与摘要

---

## 8. 测试流程

```
1. 构建 evspsegnet，eval 模式，移到 GPU
2. 加载 best_iou_seed37.pt 权重
3. 加载 EV-UAV test 数据集
4. 遍历 test DataLoader：
   ├── 前向传播 → (preds, voxel)
   ├── preds[p2v_map] 映射回事件点
   ├── 保存 seg_pred 和 seg_gt
   └── 若 cfg.roc=True，调用 roc_update 收集 Pd/Fa 统计
5. 最终输出四项指标：
   ├── iou（mIoU）
   ├── seg_acc（分割准确率）
   ├── pd（检测率）
   └── fa（误报率）
```

### 评估指标说明

- **mIoU**：仅对标签=1（目标事件）计算 Mean IoU，默认阈值 0.9
- **seg_acc**：目标事件被正确预测的比例
- **Pd（Probability of Detection）**：基于 `pd_detT=50` 像素距离阈值，将预测映射到 `260×346` 图像后做连通域统计
- **Fa（False Alarm）**：误报率，同样在图像网格上统计
- **correct_thresh**：`0.0001`

---

## 9. 结果汇总

### 9.1 Baseline 测试集结果

已跑通的 baseline（50 epochs，max_events_num=20000）在 test 集上的结果：

| 指标 | 值 |
|------|-----|
| **IoU** | 0.33155128359794617 |
| **Seg Acc** | 0.7382189631462097 |
| **Pd** | 0.858645753634277 |
| **Fa** | 0.010336109140631851 |

### 9.2 验证集对比：Baseline vs LeakyReLU

在 val 集（24 个样本）上分别评估 baseline(ReLU) 和 LeakyReLU 权重，产物位于 `log/versions/v0.0.3-val-compare/results/`。

| 指标 | Baseline (ReLU) | LeakyReLU | Delta (LeakyReLU - Baseline) |
|------|------------------|-----------|-------------------------------|
| **IoU** | 0.32399630546569824 | 0.35111120343208313 | +0.02711489796638489 |
| **Seg Acc** | 0.7172472476959229 | 0.7100265622138977 | -0.0072206854820251465 |
| **Pd** | 0.8135237295254095 | 0.7963040739185216 | -0.01721965560688791 |
| **Fa** | 0.0027119136579062384 | 0.0012747462506434986 | -0.0014371674072627398 |

简要观察：
- LeakyReLU 的验证集 IoU 更高，约提升 8.4%。
- LeakyReLU 的 FA 更低，误报率约减半。
- LeakyReLU 的 Seg Acc 和 Pd 略低于 baseline。

`log/versions/v0.0.3-val-compare/results/` 中包含：
- `val_metrics_comparison.png`：四指标分组柱状图
- `val_metrics_combined.png`：合并柱状图
- `val_compare_results.csv`：指标 CSV
- `val_compare_summary.txt`：文字汇总和 Delta
- `baseline_results.json`、`leaky_results.json`：结构化结果
- `baseline_val.log`、`leaky_val.log`：验证运行日志

### 9.3 验证集阈值扫描：同 PD 比 FA / 同 FA 比 PD

在 val 集上对两组模型扫描阈值 `0.05..0.95`，步长 `0.05`，并复用 `utils/eval.py` 的 PD/FA 指标逻辑。产物位于 `log/versions/v0.0.5-threshold-scan/results/`。

同一 PD 下比较 FA（越低越好）：

| 目标 PD | Baseline FA | LeakyReLU FA | Delta FA |
|---------|-------------|--------------|----------|
| 0.75 | 0.0017328786291560825 | 0.0011243439489069173 | -0.0006085346802491651 |
| 0.80 | 0.0025034843823640565 | 0.0014064855750754303 | -0.0010969988072886262 |
| 0.85 | 0.005200860452014046 | 0.003385177122711474 | -0.001815683329302572 |

同一 FA 下比较 PD（越高越好）：

| 目标 FA | Baseline PD | LeakyReLU PD | Delta PD |
|---------|-------------|--------------|----------|
| 0.001 | 无法插值 | 无法插值 | — |
| 0.002 | 0.767331908679108 | 0.8166509540888512 | +0.04931904540974319 |
| 0.005 | 0.8470563291297919 | 0.8655368459758664 | +0.018480516846074546 |
| 0.010 | 0.8789314800658503 | 0.8948267656203223 | +0.01589528555447195 |

简要观察：
- 在目标 PD=0.75/0.80/0.85 时，LeakyReLU 的 FA 均低于 baseline。
- 在目标 FA=0.002/0.005/0.010 时，LeakyReLU 的 PD 均高于 baseline。
- `FA=0.001` 低于当前扫描结果可稳定覆盖的插值范围，因此没有给出同 FA 下的 PD。

`log/versions/v0.0.5-threshold-scan/results/` 中包含：
- `pd_fa_curve.png`：PD-FA 曲线
- `metrics_vs_threshold.png`：阈值-指标曲线
- `fa_at_same_pd.png`：同 PD 下 FA 对比
- `pd_at_same_fa.png`：同 FA 下 PD 对比
- `threshold_scan_all.csv`：两组模型完整阈值扫描表
- `same_pd_compare.csv`、`same_fa_compare.csv`：插值对比结果
- `baseline_ReLU_threshold_scan.csv`、`LeakyReLU_threshold_scan.csv`：单模型阈值扫描结果
- `scan_thresholds.py`：本次阈值扫描脚本
- `threshold_scan_summary.txt`：文字摘要

### 9.4 样本池配比实验：A/B/C/D 四组对比 (v0.1.0)

基于 LeakyReLU 模型对训练集 99 个样本进行分类（困难负样本、时空结构背景、均匀背景各 33 个），按不同配比构建四个样本池并分别训练 50 epochs。产物位于 `log/versions/0.1.0-sample-pool-ratio-experiments/`。

样本池配比：

| 组别 | 困难负样本 | 时空结构背景 | 均匀背景 | 说明 |
|------|-----------|-------------|---------|------|
| A | 25% | 25% | 50% | 偏均匀 |
| B | 35% | 30% | 35% | 平衡版 |
| C | 45% | 30% | 25% | 偏困难负样本 |
| D | 35% | 40% | 25% | 偏时空结构 |

验证集结果（threshold=0.9）：

| 指标 | A (25/25/50) | B (35/30/35) | C (45/30/25) | D (35/40/25) |
|------|-------------|-------------|-------------|-------------|
| **IoU** | 0.3577 | 0.3715 | 0.3450 | **0.3731** |
| **Seg Acc** | **0.7228** | 0.7097 | 0.7101 | 0.7174 |
| **PD** | 0.8049 | 0.8022 | **0.8123** | 0.8026 |
| **FA** | 0.001264 | **0.000960** | 0.002371 | 0.001054 |

简要观察：
- **D 组（偏时空结构）IoU 最高** 0.3731，时空结构背景比例提升对分割精度最有利
- **B 组（平衡版）FA 最低** 0.00096，误报最少，IoU 第二高 0.3715
- **C 组（偏困难负样本）PD 最高** 0.8123，但 FA 也最高 0.00237、IoU 最低 0.3450
- A 组（偏均匀）Seg Acc 最高 0.7228，其他指标居中
- 与 Baseline(ReLU) IoU=0.324 和 LeakyReLU IoU=0.351 相比，各组均有提升

`log/versions/0.1.0-sample-pool-ratio-experiments/` 中包含：
- `charts/group_epoch_loss_comparison.png`：四组 epoch 平均 loss 曲线
- `charts/group_val_metrics_bar.png`：四指标分组柱状图
- `charts/group_pd_fa_comparison.png`：PD/FA 双轴柱状图
- `group_val_results.csv`、`group_val_summary.txt`：评估汇总
- `A/B/C/D/models/best_iou_seed37.pt`、`best_loss_seed37.pt`：各组模型权重
- `A/B/C/D/loss_records.csv`、`epoch_loss_summary.csv`：结构化训练日志
- `run_group_experiments.py`：训练评估自动化脚本

---

## 10. 当前适配改动

为在 RTX 2060 SUPER 8GB 上跑通 baseline，已做以下修改：

### 10.1 配置文件路径适配

`configs/evisseg_evuav.yaml`：

| 字段 | 原始值 | 当前值 |
|------|--------|--------|
| `DATA.root` | `/home/a/chennuo/datasets/EV-UAV-dataset` | `/root/EV-UAV-dataset` |
| `TRAIN.model_save_root` | `/home/a/chennuo/EV-UAV-main/log/model` | `/root/EV-UAV/log/model` |
| `TEST.model_path` | `/home/a/chennuo/EV-UAV-main/log/model/best_iou_seed37.pt` | `/root/EV-UAV/log/model/best_iou_seed37.pt` |

`configs/configs.py`：

| 字段 | 原始值 | 当前值 |
|------|--------|--------|
| `default` config | `/home/a/chennuo/EV-UAV-main/configs/evisseg_evuav.yaml` | `/root/EV-UAV/configs/evisseg_evuav.yaml` |

### 10.2 训练参数调整

`configs/evisseg_evuav.yaml` 中 `TRAIN.max_events_num`：

| 原始值 | 当前值 | 说明 |
|--------|--------|------|
| 150000 | 20000 | 降低单帧事件数以适配 8GB 显存 |

### 10.3 spconv 卷积算法

所有 spconv 卷积层（`model/evspsegnet.py`、`model/basemodel.py`）的卷积算法（`algo`）已统一设为 `spconv.ops.ConvAlgo.Native`，以确保与 spconv 2.1.21 预编译包兼容，并支持 RTX 2060 SUPER 的 sm_75 架构。

### 10.4 spconv 版本选型

- spconv-cu111 2.x 系列预编译包不支持 sm_75 的 `MaskImplicitGemm` kernel
- 改为从 GitHub 源码编译 spconv 1.2.1 并创建 `spconv.pytorch` 兼容层
- 最终回到 spconv-cu111 2.1.21 + Native 算法方案，保持与原始 Dockerfile 一致

### 10.5 Git 与远程仓库状态

| 项目 | 当前状态 |
|------|----------|
| 远程仓库 | `git@github.com:KnightKanox/-_sult_EV-UAV.git` |
| baseline 分支 | `main` |
| LeakyReLU 实验分支 | `experiment/leaky-relu` |
| baseline 关键提交 | `18c8620` |
| LeakyReLU 激活函数提交 | `f2c75d5` |
| 实验图表/验证对比提交 | `5db21ac` |
| 最新提交内容 | LeakyReLU 全量训练 + Baseline 验证集对比 |

---

## 11. 已知问题与注意事项

1. **显存限制**：RTX 2060 SUPER 仅 8GB 显存，`max_events_num` 必须 ≤ 20000 才能避免 CUDA OOM。若更换更大显存 GPU，可将此值调高以获得更好结果。
2. **spconv kernel 兼容性**：预编译 spconv-cu111 的 MaskImplicitGemm kernel 不支持 sm_75，必须使用 `ConvAlgo.Native`。
3. **Python 环境**：容器默认 Python 是 `/usr/bin/python3`，不含项目依赖。必须使用 conda 环境 `evuav` 下的 Python（`/root/exit/envs/evuav/bin/python`）。
4. **train_workers 未生效**：配置文件中 `train_workers: 8` 存在，但 `train.py` 的 DataLoader 创建时未传入 `num_workers` 参数，实际使用单进程加载。
5. **确定性模式**：训练开启了 `torch.use_deterministic_algorithms(True)`，对某些算法可能有轻微性能影响，但确保结果可复现。
6. **模型加载路径**：测试时必须使用容器内路径 `/root/EV-UAV/log/model/best_iou_seed37.pt`，不能使用宿主机路径。
7. **稀疏空间固定**：`custom_collate` 中 `spatial_shape` 硬编码为 `[11*32, 9*32, 256*32]`，修改此项可能需要重新设计模型或调整输入。
8. **评估阈值**：mIoU 默认阈值 0.9，Pd 的 detT 为 50 像素，这两项对最终指标有显著影响。
9. **Python 退出阶段告警**：训练、验证和部分统计脚本结束时可能出现 `free(): invalid pointer`，当前观察为 spconv/扩展库退出阶段释放问题；训练循环、权重文件、图表和指标均已正常生成。
10. **验证集公平对比方式**：baseline 评估时需使用 ReLU 模型结构加载 baseline 权重，LeakyReLU 评估时需使用 LeakyReLU 模型结构加载 LeakyReLU 权重，否则权重结构虽然可加载但实验语义不一致。

---

## 12. 快速验证命令

```bash
# 确认容器运行中
docker ps | grep evuav

# 验证环境
docker exec evuav bash -i -c "conda activate evuav && python -c 'import torch; import spconv.pytorch; import HAIS_OP; print(\"ALL OK\")'"

# 确认 baseline 模型文件
docker exec evuav bash -c "ls -lh /root/EV-UAV/log/model/best_iou_seed37.pt"

# 确认 LeakyReLU 模型文件
docker exec evuav bash -c "ls -lh /root/EV-UAV/log/model_leaky_relu/best_iou_seed37.pt"

# 重新训练当前分支配置
docker exec evuav bash -i -c "conda activate evuav && cd /root/EV-UAV && python train.py"

# 重新测试当前配置
docker exec evuav bash -i -c "conda activate evuav && cd /root/EV-UAV && python test.py"

# 查看 LeakyReLU 训练图表
afd-open /media/sult/山江冬夏/训练集、验证集/EV-UAV/log/versions/v0.0.2-leaky-relu-training/charts

# 查看验证集对比图表
afd-open /media/sult/山江冬夏/训练集、验证集/EV-UAV/log/versions/v0.0.3-val-compare/results

# 查看阈值扫描对比图表
afd-open /media/sult/山江冬夏/训练集、验证集/EV-UAV/log/versions/v0.0.5-threshold-scan/results
```
